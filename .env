# ---- Celery broker results (unchanged) --------------------
CELERY_BROKER_URL=redis://redis:6379/0
CELERY_RESULT_BACKEND=redis://redis:6379/1

# ---- Gmail SMTP ------------------------------------------
SMTP_HOST=
SMTP_PORT=465              # 465 works too but uses implicit TLS
SMTP_USE_TLS=true
SMTP_USER=
SMTP_PASSWORD=
FROM_EMAIL=

# ---- Brand / contact meta --------------------------------
COMPANY_NAME=Poridhi
CONTACT_NUMBER=+8801XXXXXXX
